[project]
name = "leets"
version = "0.0.0"
description = "LeetCode solutions to pandas problems."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "pandas>=3.0.3",
  "pytest>=9.1.1",
]
